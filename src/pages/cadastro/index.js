import React from 'react';
import {
  Container,
} from './styles';

const Cadastro = () => {
  const title = 'CADASTRO FORM';
  return (
    <Container>
      {title}
    </Container>
  );
};

export default Cadastro;
